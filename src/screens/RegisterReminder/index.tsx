import React from 'react';
import { SafeAreaView, Text, View } from 'react-native';
import { StatusBar } from 'expo-status-bar';

export default function RegisterReminder(){
    return (
        <SafeAreaView>
            <StatusBar style='auto' />
            <Text>Registrar contas</Text>
        </SafeAreaView>
    )
}